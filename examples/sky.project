// SkyLang project manifest. The build activates the profile named here;
// changing this one line retargets the project. The requires block
// (dependency registry) arrives with that feature.
project shop
profile jvm-jakarta
